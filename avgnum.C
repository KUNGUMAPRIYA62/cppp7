#include<stdio.h>
void main()
{
int i,n,k,avg,sum;
printf("enter the n values:");
scanf("%d",&n);
for(i=1;i<=n;i++)
{
printf("enter the array of n values:");
scanf("%d",&k[i]);
}
for(i=1;i<=n;i++)
{
sum=sum+k[i];
}
avg=sum/n;
printf("avg:%d",avg);
}
